apiVersion: v1
kind: Service
metadata:
  name: "{{ .Chart.Name }}-service"
  labels:
    app: "{{ .Chart.Name }}"
    chart: "{{ .Chart.Name }}-{{ .Chart.Version }}"
    release: "{{ .Release.Name }}"
    heritage: "{{ .Release.Service }}"
  {{- with .Values.service.annotations }}
  annotations: {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.service.type }}
  {{- with .Values.service.externalIPs }}
  externalIPs: {{ . | toYaml | nindent 4 }}
  {{- end }}
  ports:
  - name: game-port
    port: {{ .Values.config.gamePort | int }}
    targetPort: game-port
    protocol: UDP
    {{- if eq .Values.service.type "NodePort" }}
    nodePort: {{ .Values.config.gamePort | int }}
    {{- end }}
  - name: game-port-2
    port: {{ int (add .Values.config.gamePort 1) }}
    targetPort: game-port-2
    protocol: UDP
    {{- if eq .Values.service.type "NodePort" }}
    nodePort: {{ int (add .Values.config.gamePort 1) }}
    {{- end }}
  - name: query-port
    port: {{ .Values.config.queryPort | int }}
    targetPort: query-port
    protocol: UDP
    {{- if eq .Values.service.type "NodePort" }}
    nodePort: {{ .Values.config.queryPort| int }}
    {{- end }}
  - name: query-port-2
    port: {{ int (add .Values.config.queryPort 1) }}
    targetPort: query-port-2
    protocol: UDP
    {{- if eq .Values.service.type "NodePort" }}
    nodePort: {{ int (add .Values.config.queryPort 1) }}
    {{- end }}
  - name: rcon-port
    port: {{ .Values.config.rconPort | int }}
    targetPort: game-port
    protocol: TCP
    {{- if eq .Values.service.type "NodePort" }}
    nodePort: {{ .Values.config.rconPort | int }}
    {{- end }}
  selector:
    app: "{{ .Chart.Name }}"
